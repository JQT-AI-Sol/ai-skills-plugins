# Document Templates

各フェーズで生成するドキュメントの汎用テンプレート（業種非依存）。

## requirement.md テンプレート

```markdown
# 要件定義書

**プロジェクト名**: {{project_name}}
**作成日**: {{date}}
**バージョン**: 1.0

## 1. プロジェクト概要

### 1.1 背景
{{memo.mdから抽出した背景情報}}

### 1.2 目的
{{解決したい課題と達成目標}}

### 1.3 スコープ
- **対象**: {{対象業務・ユーザー}}
- **対象外**: {{スコープ外の項目}}

## 2. ステークホルダー

| 役割 | 担当 | 備考 |
|------|------|------|
| {{役割}} | {{担当者}} | {{備考}} |

## 3. 機能要件

### F-001: {{機能名}}
- **優先度**: Must / Should / Could
- **概要**: {{機能の概要}}
- **ユーザーストーリー**: {{役割}}として、{{操作}}したい。なぜなら{{理由}}。
- **受入条件**:
  - [ ] {{条件1}}
  - [ ] {{条件2}}

### F-002: {{機能名}}
...

## 4. 非機能要件

### NF-001: パフォーマンス
- {{レスポンスタイム、同時接続数など}}

### NF-002: セキュリティ
- {{認証方式、データ保護など}}

### NF-003: 可用性
- {{稼働率、バックアップなど}}

## 5. 制約条件

- **技術**: {{技術的な制約}}
- **予算**: {{予算制約}}
- **期限**: {{スケジュール制約}}

## 6. 用語集

| 用語 | 定義 |
|------|------|
| {{用語}} | {{定義}} |
```

---

## design.md (screen-design.md) テンプレート

```markdown
# 画面設計書

**プロジェクト名**: {{project_name}}
**作成日**: {{date}}

## 1. 画面一覧

| 画面ID | 画面名 | URL | 対応要件 |
|--------|--------|-----|---------|
| SCR-001 | {{画面名}} | /{{path}} | F-001 |

## 2. 画面遷移図

{{mermaid記法またはテキストベースの遷移図}}

## 3. 画面詳細

### SCR-001: {{画面名}}

**対応要件**: F-001
**設計ID**: S-001

**レイアウト**:
{{画面構成の説明}}

**要素一覧**:
| 要素 | 種類 | 動作 |
|------|------|------|
| {{要素名}} | ボタン/入力/表示 | {{動作説明}} |

**バリデーション**:
- {{入力チェックルール}}

**状態遷移**:
- 初期状態 → {{操作}} → {{次の状態}}
```

---

## db-schema.md テンプレート

```markdown
# データベース設計書

**プロジェクト名**: {{project_name}}
**DB**: Supabase (PostgreSQL)

## 1. テーブル一覧

| テーブル名 | 説明 | 対応要件 |
|-----------|------|---------|
| {{table_name}} | {{説明}} | F-001 |

## 2. テーブル定義

### {{table_name}}

**設計ID**: S-001

| カラム名 | 型 | NULL | デフォルト | 説明 |
|---------|------|------|----------|------|
| id | uuid | NO | gen_random_uuid() | PK |
| created_at | timestamptz | NO | now() | 作成日時 |
| {{column}} | {{type}} | {{nullable}} | {{default}} | {{説明}} |

**インデックス**:
- `idx_{{table}}_{{column}}` ON {{column}}

**RLS ポリシー**:
```sql
-- {{ポリシー説明}}
CREATE POLICY "{{policy_name}}" ON {{table_name}}
  FOR {{operation}} USING (auth.uid() = user_id);
```

## 3. ER図

```mermaid
erDiagram
    {{table1}} ||--o{ {{table2}} : "{{relation}}"
```
```

---

## api-design.md テンプレート

```markdown
# API設計書

**プロジェクト名**: {{project_name}}
**ベースURL**: /api

## 1. エンドポイント一覧

| メソッド | パス | 説明 | 対応要件 | 設計ID |
|---------|------|------|---------|--------|
| GET | /api/{{resource}} | {{説明}} | F-001 | S-001 |

## 2. エンドポイント詳細

### GET /api/{{resource}}

**設計ID**: S-001
**対応要件**: F-001
**認証**: 要 / 不要

**リクエスト**:
| パラメータ | 型 | 必須 | 説明 |
|-----------|------|------|------|
| {{param}} | string | Yes | {{説明}} |

**レスポンス (200)**:
```json
{
  "data": [
    {
      "id": "uuid",
      "{{field}}": "{{value}}"
    }
  ]
}
```

**エラーレスポンス**:
| コード | 説明 |
|--------|------|
| 401 | 認証エラー |
| 404 | リソース未存在 |
```
```

---

## review-report.md テンプレート

[consistency-review-checklist.md](consistency-review-checklist.md) の「Review Report Format」セクションを参照。
